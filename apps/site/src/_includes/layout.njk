<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or site.appName or '11ty Gamification Blog' }}</title>
  <meta name="generator" content="Eleventy">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Ctext y='50' x='8' font-size='48'%3E%F0%9F%8C%B1%3C/text%3E%3C/svg%3E" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Literata:opsz,wght@7..72,400;7..72,600;7..72,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/styles.css" />
  <style>
    :root {
      --gradient-primary: linear-gradient(135deg, #7c3aed 0%, #22c55e 100%);
      --gradient-accent: linear-gradient(135deg, #06b6d4 0%, #f59e0b 100%);
      --text-color: #0f172a;
      --bg-color: #ffffff;
      --header-bg: #0f172a; /* slate-900 */
      --footer-bg: #111827; /* gray-900 */
      --header-fg: #e5e7eb; /* gray-200 */
      --footer-fg: #e5e7eb; /* gray-200 */
    }
    html { font-family: 'Literata', serif; color: var(--text-color); background: var(--bg-color); }
    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      background: radial-gradient(1200px 600px at -10% -10%, rgba(124,58,237,0.08), transparent 60%),
                  radial-gradient(900px 500px at 110% -20%, rgba(34,197,94,0.08), transparent 60%),
                  var(--bg-color);
    }
    .page-main { flex: 1 0 auto; }
    .gradient-text { background: var(--gradient-primary); -webkit-background-clip: text; background-clip: text; color: transparent; }
    .badge { font-size: 2rem; display: inline-block; margin: 0.25rem; }
    .badge.spirit { filter: grayscale(100%) brightness(0.7) contrast(1.2); opacity: 0.8; position: relative; }
    .badge.spirit::after { content: "👻"; position: absolute; font-size: 0.5em; top: -5px; right: -5px; z-index: 1; }
    .container { width: 100%; max-width: 72rem; margin: 0 auto; padding: 1rem; }
    .topnav { display: flex; align-items: center; justify-content: space-between; gap: 1rem; }
    .navlinks a { margin-left: 1rem; text-decoration: none; color: #334155; border-bottom: 2px solid transparent; padding-bottom: 2px; }
    .topnav .navlinks a { color: var(--header-fg); }
    .navlinks a:hover { border-color: #22c55e; }
    .card { background: #fff; border: 1px solid #e5e7eb; border-radius: 0.5rem; padding: 0.75rem 1rem; box-shadow: 0 1px 2px rgba(0,0,0,0.04); }
    /* Heatmap sizing */
    [data-heatmap="calendar"] { --cell-size: 56px; }
    @media (max-width: 768px) { [data-heatmap="calendar"] { --cell-size: 40px; } }
    @media (max-width: 480px) { [data-heatmap="calendar"] { --cell-size: 28px; } }
  </style>
</head>
<body>
  <header style="background: var(--header-bg); color: var(--header-fg);">
    <div class="container topnav">
      <a href="/" class="gradient-text" style="font-weight:700; font-size:1.5rem; text-decoration:none; color: var(--header-fg);">{{ site.appName or '11ty Gamification Blog' }}</a>
      <nav class="navlinks" aria-label="Primary">
        <a href="/posts/">Posts</a>
        <a href="/badges/">Badges</a>
        <a href="/tags/">Tags</a>
        <a href="/search/">Search</a>
      </nav>
    </div>
  </header>
  <main class="container page-main">
    {{ content | safe }}
  </main>
  <footer style="background: var(--footer-bg); color: var(--footer-fg);">
    <div class="container" style="font-size:0.9rem; padding-top:1.25rem; padding-bottom:1.25rem;">
      <p style="margin:0 0 0.4rem 0;">Made with 🌱 to make writing feel like play.</p>
      <p style="margin:0 0 0.4rem 0;">
        Code licensed <strong>MIT</strong>; Content licensed <strong>CC BY-NC-SA 4.0</strong>.
        © {{ site.year }} <a class="focus-ring" style="color:#93c5fd; text-underline-offset:2px;" href="{{ site.author.url }}">{{ site.author.name }}</a>.
        <a class="focus-ring" style="color:#93c5fd; text-underline-offset:2px;" href="{{ site.repoUrl }}">Source on GitHub</a>.
      </p>
      <p style="margin:0;">
        Inspired by <a class="focus-ring" style="color:#93c5fd; text-underline-offset:2px;" href="{{ site.inspiration.url }}">{{ site.inspiration.name }}</a> and {{ site.inspiration.person }}.
      </p>
    </div>
  </footer>
</body>
</html>
