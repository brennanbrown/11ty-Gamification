{
  "name": "@indiegarden/site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build:css -- --watch & eleventy --serve",
    "build": "npm run build:css && eleventy",
    "build:css": "tailwindcss -i ./src/assets/tailwind.css -o ./src/assets/styles.css -c tailwind.config.cjs",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.40",
    "tailwindcss": "^3.4.10"
  }
}
